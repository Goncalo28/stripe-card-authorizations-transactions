import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  text: {
    paddingTop: 12,
    paddingBottom: 12,
    marginLeft: 24,
    color: "#1d1f1f",
    fontSize: 16,
    fontWeight: "600",
  },
});

export default styles;
